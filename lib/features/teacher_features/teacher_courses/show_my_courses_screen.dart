import 'package:flutter/material.dart';

class ShowTeacherCourses extends StatelessWidget {
  ShowTeacherCourses({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("corses screen"));
  }
}
